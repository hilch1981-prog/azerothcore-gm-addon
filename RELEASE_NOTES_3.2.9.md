# AzerothAdmin 3.2.9-335a 변경 내역

## 파티 및 봇 퀘스트 동기화

- 퀘스트 도우미에 `없는 퀘스트 자동 추가`와 `본캐 조건완료 시 봇 완료` 선택 항목을 추가했다.
- 기존 진행 퀘스트를 일괄 동기화하고, 이후 새 파티원·봇 합류와 새 퀘스트 등록을 자동 감지한다.
- 없는 퀘스트는 WotLK 기본 `QuestLogPushQuest`로 공유하므로 일반 파티원과 최신 Playerbots의 퀘스트 공유 처리기를 함께 사용한다.
- 조건완료 전환은 `quest complete [퀘스트 링크]` 파티/공격대 명령을 지원하는 Playerbots 빌드에 전달한다.
- 전송 큐, 0.35초 간격과 퀘스트별 중복 제거로 대량 동기화 시 명령 폭주를 방지한다.
- 퀘스트 보상 선택·수령은 자동화하지 않는다.

## 호환성 제한

- 현재 최신 upstream `mod-playerbots`는 기본 퀘스트 공유를 처리하지만 `quest complete` 채팅 명령은 제공하지 않는다. 이 경우 서버의 `AiPlayerbot.SyncQuestWithPlayer = 1` 설정으로 퀘스트 반납 시 동기화한다.
- 구버전/호환 Playerbots에서 `quest complete [퀘스트 링크]`를 제공하는 경우에만 본캐의 조건완료 순간 강제 완료가 동작한다.
- 일반 플레이어 파티원에게는 퀘스트 공유만 전달되며 강제 완료 명령은 적용되지 않는다.
- 파티원이 퀘스트 요구 조건, 선행 퀘스트, 레벨 또는 퀘스트 로그 여유를 충족하지 못하면 서버가 수락을 거부할 수 있다.
