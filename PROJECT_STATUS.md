# 프로젝트 상태

## 현재 확인된 환경

- 대상: WoW WotLK 3.3.5a Build 12340
- 서버: AzerothCore WotLK
- 스크립트: Lua 5.1
- 언어/UI: koKR 중심

## 소스 확보 상태

2026-08-22에 사용자 지정 ZIP `GMAddon_335a_AzerothCore_V328_20260818.zip`을 최신 기준 소스로 확보했다.

- 원본 수정 시각: 2026-08-18 04:36:53 KST
- SHA-256: `54A79A0DF20043EBC98615E9F7021751614AA273F41B90FF91E4A7CA17E2972B`
- 애드온 폴더: `AzerothAdmin/`
- 애드온 버전: `3.2.8-335a`
- TOC 인터페이스: `30300`
- 포함 파일: 총 70개(그중 Lua 32개, XML 2개, TOC 1개)

이전에 제공된 `WOW_Legends_KR_FULL_ALL_20260813.zip`은 한국어 DB 패치 SQL이며 애드온 소스가 아니다. `지엠커멘드.sql` 역시 AzerothCore `command` 테이블 참고 덤프이므로 이번 저장소 소스에 포함하지 않았다.

## 확인된 요구사항

- 퀘스트 보상 지역 분류에서 동부 왕국/칼림도어 0건 문제 수정
- 고정 보상과 선택 보상 모두 반영
- `$f`, `$f1`, `$f(...)` 같은 내부 로컬라이징 토큰의 UI 노출 제거
- 전문기술별/장비 부위별 아이템 강화 분류 복구
- 낚시 도안, 교본, 학습 아이템 분류 추가
- InvenCraftInfo 단일 메인 프레임화 및 상태 초기화
- 불필요한 원격 은행 확인창 제거
- 실제 원격 은행 UI 가능 여부를 AzerothCore와 3.3.5a 제약 기준으로 검증
- 사망/유령 상태에서 자기 부활 명령 전달 방식 검증
- 아이템 목표의 NPC/오브젝트 드랍처 이동 및 순환
- 주요 팝업을 최상위 레이어로 유지

## 현재 정적 확인 결과

- TOC에 WotLK 3.3.5a용 `Interface: 30300`과 버전 `3.2.8-335a`가 선언되어 있다.
- 금지 API `C_Container`, `C_Item`, `ScrollBox`, `C_QuestLog`의 소스 사용은 검색되지 않았다.
- 비밀정보 패턴 검색 결과 실제 자격 증명은 발견되지 않았다. 명령 도움말에 비밀번호/토큰이라는 일반 문구는 존재한다.
- 원본에 포함된 테스트 보고서는 TOC 경로 29개 누락 0건, Lua 32개 구문 검사 통과를 기록하고 있다.
- GitHub Actions에서 Lua 5.1 문법, TOC 경로, Retail 전용 API, 명령 메타데이터 감사를 자동 수행한다.
- 공식 AzerothCore 기준 명령 699개와 애드온 명령 메타데이터 740개를 대조하며, 필수 모듈 명령 41개의 누락과 권한 불일치를 검사한다.
- 기존 퀘스트 보상 지역 분류는 노스렌드 1,339개, 아웃랜드 806개, 칼림도어 74개, 동부 왕국 136개의 유효한 3.3.5a 아이템을 표시했다.
- 공식 `quest_template`의 고정/선택 보상을 `AreaTable.dbc`의 대륙 정보와 통합해 노스렌드 1,380개, 아웃랜드 1,175개, 칼림도어 878개, 동부 왕국 910개로 보강했다.
- `$f{호드/얼라이언스}` 진영 토큰의 깨진 괄호·구분자 10건을 교정하고, 비표준 토큰이 다시 들어오지 않도록 CI 감사를 추가했다.
- 내장 `RecipeDB`/`SpellDB` 연결을 확장해 요리 레시피 141개와 응급치료 처방전 7개를 전문기술 목록에 복구했다.
- 낚시 교본과 낚시 관련 주문식을 별도 분류하며, 기계공학 망토·장화 및 대장기술 손목·장갑 전용 강화를 장비 부위와 직업별 모음에 함께 연결했다.
- 전문기술/강화 분기의 필수 키와 내장 데이터 최소 수량을 검증하는 자동 테스트를 GitHub Actions에 추가했다.
- InvenCraftInfo는 `AzerothAdminCraftInfoFrame` 하나만 표시하고 공통 관리 프레임 경로로 열리도록 정리해, 닫을 때 이전 AzerothAdmin 창으로 돌아갈 수 있게 했다.
- 전문기술 창을 일반 경로로 다시 열면 검색어와 입력 포커스, 정렬 방식, 페이지, 상세 선택 및 임시 로딩 상태를 초기화하도록 보강했다.
- 원격 은행은 확인 목록창 없이 `.character check bank`로 서버 세션을 요청하고 `BANKFRAME_OPENED` 이후 기본 `BankFrame`을 표시한다. 2026-08-22 사용자 게임 내 실사용으로 정상 동작을 확인했다.

## 최근 기능 고도화

- 티켓 관리: 승격 목록, 담당자 지정/해제, GM 메모, 답변 조회/추가/삭제, 티켓 승격을 추가했다.
- 그룹 관리: 구성원 목록, 참가, 그룹장 변경, 제거, 전체 부활, 해산을 별도 메뉴로 추가했다.
- `group join`은 실제 AzerothCore C++ 구현에 맞춰 기존 그룹 구성원과 참가시킬 플레이어의 두 이름을 모두 요구한다.
- 공식 명령 메타데이터에 없는 `group invites`와 서버 전체 상태에 영향을 주는 티켓 전역 명령은 메뉴에서 제외했다.

## 완료로 간주할 수 없는 항목

게임 클라이언트와 AzerothCore 서버를 이용한 실동작 회귀 테스트는 이번 GitHub 인수인계 준비 과정에서 수행하지 않았다. 요구사항별 구현 완료 여부는 소스 분석과 게임 내 검증 후 확정해야 한다.
