# 개발 규칙

## 호환성

- WoW WotLK 3.3.5a Build 12340의 API와 이벤트만 사용한다.
- Lua 5.1에서 동작하지 않는 문법과 라이브러리를 사용하지 않는다.
- AzerothCore WotLK의 공개 소스에서 GM 명령의 이름, 보안 등급, 인수 및 처리 경로를 확인한다.
- 다른 코어 또는 Retail 구현은 아이디어 참고에만 사용하고 그대로 복사하지 않는다.

## 변경 원칙

- 한 커밋에는 가능한 한 한 기능만 포함한다.
- 한 PR에는 하나의 기능 모듈 또는 하나의 공통 기반 변경만 포함한다.
- 리팩터링과 기능 수정을 분리한다.
- 기존 파일을 삭제하거나 이름을 바꿔야 한다면 사유와 영향 범위를 먼저 기록한다.
- 데이터 생성물이 크면 생성 절차와 출처를 문서화하고, 불필요한 바이너리 중복을 피한다.
- 데이터베이스 덤프, 계정 정보, 접속 정보, API 키, 개인 경로 및 서버 비밀값을 커밋하지 않는다.

## 모듈 원칙

- `MODULE_MANIFEST.json`을 기능 소유권의 기준으로 사용한다.
- 작업 시작 시 `tools/module_context.py`로 최소 파일 목록을 확인한다.
- 공통 프레임워크가 기능별 GM 명령, 좌표, 데이터 또는 UI 구현을 직접 소유하지 않는다.
- 모듈 간 직접 monkey patch 대신 명시적인 공개 함수, 등록 함수 또는 이벤트를 사용한다.
- 대용량 데이터는 로직·UI 파일과 분리하고 데이터 변경이 없으면 검토 대상에서 제외한다.
- legacy 파일 이동은 기능별로 진행하고 같은 PR에서 여러 모듈을 동시에 이동하지 않는다.

## 다국어 원칙

- enUS가 기본 fallback이다.
- koKR, zhCN, zhTW 팩은 동일한 키 집합을 유지한다.
- 새 기능 문자열은 해당 모듈 locale 파일에 둔다.
- 번역되지 않은 키는 enUS를 표시하며 nil 또는 빈 UI가 되지 않게 한다.
- 게임 데이터 고유명사는 UI 문자열과 분리한다.

## UI 원칙

- 기본 `Frame`, `ScrollFrame`, `FrameStrata`, `FrameLevel` 등 3.3.5a 지원 수단을 사용한다.
- 아이템 정보창의 다크/골드 디자인은 InvenCraftInfo의 시각 참고만 하며 로직과 창은 합치지 않는다.
- 창 전환 시 이전 목록, 선택, 상세 및 재료 상태를 명시적으로 초기화한다.
- 사용자에게 의미 없는 내부 확인창이나 가짜 기능 UI를 노출하지 않는다.

## 출처 및 라이선스

- 외부 코드·번역·애드온·데이터·UI 리소스의 원본 주소, 저작자, 라이선스, 사용 범위와 변경 여부를 기록한다.
- 필요한 저작권 고지와 NOTICE를 보존한다.
- 출처 또는 라이선스를 확인할 수 없는 리소스는 배포본에 새로 포함하지 않는다.

## 검증 체크리스트

1. 모든 Lua 파일을 Lua 5.1 기준으로 파싱한다.
2. TOC 항목을 순서대로 읽어 파일 존재와 대소문자를 확인한다.
3. `C_Container`, `C_Item`, `ScrollBox`, 최신 `C_QuestLog` 등 금지 API를 검색한다.
4. XML의 Lua/XML 참조가 실제 경로와 일치하는지 확인한다.
5. locale pack 키의 일치 여부를 검사한다.
6. 기능별 재현 절차와 기대 결과를 기록한다.
7. ZIP을 새 임시 위치에 풀어 최상위 폴더, TOC 및 전체 파일을 다시 검사한다.

## Git 규칙

- 관련 파일만 명시적으로 스테이징한다.
- 무관한 사용자 변경을 포함하지 않는다.
- 대규모 일괄 스테이징을 사용하지 않는다.
- 푸시 전 상태, diff, 원격 저장소 및 브랜치를 확인한다.
